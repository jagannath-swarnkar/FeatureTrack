import React from "react";

const Loader = () => {
    return (
        <div className="lds-container">
            <div className="lds-loader"></div>
        </div>
    );
};

export default Loader;
